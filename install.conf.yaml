- defaults:
    link:
      relink: true

- link:
    ~/.config/nvim: nvim
    ~/.tmux.conf: tmux/tmux.conf
    ~/.zshrc: zsh/zshrc
    ~/.config/sway: sway
    ~/.config/waybar: desktop/waybar
    ~/.gitconfig: git/gitconfig
    ~/.config/kitty: kitty
