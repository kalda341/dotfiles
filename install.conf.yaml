- defaults:
    link:
      relink: true

- link:
    ~/.config/nvim: nvim
    ~/.tmux.conf: tmux/tmux.conf
    ~/.zshrc: zsh/zshrc
    ~/.config/sway: sway
    ~/.config/waybar: waybar
    ~/.gitconfig: git/gitconfig
    ~/.config/kitty: kitty
    ~/.config/foot: foot
